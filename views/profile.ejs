<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Currency Converter</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
    <!-- Include additional styles if needed -->
</head>
<body class="bg-gray-100 p-8">
  
    <p class="" href="#">
      username: <%= user.userName %>
    </p>
  
  
    <div class="max-w-md mx-auto bg-white p-8 rounded shadow-md">
        <!-- Conversion Form -->
        <form action="/convert" method="post" class="mb-6">
            <div class="flex items-center mb-4">
                <!-- Currency From -->
                <div class="mr-2">
                    <label for="fromCurrency" class="block text-gray-700">From Currency</label>
                    <div class="relative">
                        <select name="fromCurrency" id="fromCurrency" class="block appearance-none w-full bg-white border border-gray-300 text-gray-700 py-2 px-4 pr-8 rounded leading-tight focus:outline-none focus:border-blue-500 focus:shadow-outline-blue">
                            <!-- Include options for different currencies with flags -->
                            <option value="usd">USD - US Dollar</option>
                            <option value="eur">EUR - Euro</option>
                            <!-- Add more options as needed -->
                        </select>
                    </div>
                </div>

                <!-- Currency To -->
                <div>
                    <label for="toCurrency" class="block text-gray-700">To Currency</label>
                    <div class="relative">
                        <select name="toCurrency" id="toCurrency" class="block appearance-none w-full bg-white border border-gray-300 text-gray-700 py-2 px-4 pr-8 rounded leading-tight focus:outline-none focus:border-blue-500 focus:shadow-outline-blue">
                            <!-- Include options for different currencies with flags -->
                            <option value="usd">USD - US Dollar</option>
                            <option value="eur">EUR - Euro</option>
                            <!-- Add more options as needed -->
                        </select>
                    </div>
                </div>
            </div>

            <!-- Amount Input -->
            <div class="mb-4">
                <label for="amount" class="block text-gray-700">Amount</label>
                <input type="number" name="amount" id="amount" class="w-full border border-gray-300 p-2 rounded">
            </div>

            <!-- Submit Button -->
            <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline-blue">Convert</button>
        </form>
        
      </div>
    <a href="/logout" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 mt-5 rounded focus:outline-none focus:shadow-outline-blue">logout</a>

    
    
    <%- include('partials/footer') -%>
</body>
</html>
